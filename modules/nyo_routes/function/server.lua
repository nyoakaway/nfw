Nfw.registerFunctions('nyofw',function()

    return {
        onAddPlayerToRoute = function(src, config, category, route)

        end,
        onRemPlayerFromRoute = function(src)

        end,
        onSuccessToCollectItem = function(user_id, config, itens)
        end,
        onFailToCollectItem = function(reason, user_id, config)
        end,
    }

end)